import{useState}from'react';import{api}from'../services/api';export default function ForgotPassword(){const[email,setEmail]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');const submit=async e=>{e.preventDefault();setErr('');setMsg('');try{const r=await api.requestPasswordReset(email);setMsg(r.message)}catch(x){setErr(x.message)}};return <div className="page-shell"><div className="panel" style={{maxWidth:520,margin:'60px auto'}}><h1>Reset your password</h1><p>Enter your EduNex email. If the account exists, a reset link can be issued.</p><form onSubmit={submit}><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" required/><button className="btn" type="submit">Request reset</button></form>{msg&&<p className="success-text">{msg}</p>}{err&&<p className="error-text">{err}</p>}</div></div>}