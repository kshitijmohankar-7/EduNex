import { useMemo, useState } from 'react';
export default function ExamPrep(){const [subject,setSubject]=useState('Java Programming');const [days,setDays]=useState(14);const [hours,setHours]=useState(2);const plan=useMemo(()=>Array.from({length:Math.min(days,7)},(_,i)=>({day:i+1,topic:['Core concepts','Arrays & Strings','OOP','Collections','Exception handling','Revision','Mock test'][i],hours})),[days,hours]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">AI & Learning</span><h1>Exam Preparation</h1><p>Build a focused revision plan around your subject, time and exam window.</p></div></div><section className="panel"><div className="form-grid"><label>Subject<input className="input" value={subject} onChange={e=>setSubject(e.target.value)}/></label><label>Days<input className="input" type="number" min="1" max="60" value={days} onChange={e=>setDays(Number(e.target.value))}/></label><label>Hours/day<input className="input" type="number" min="1" max="12" value={hours} onChange={e=>setHours(Number(e.target.value))}/></label></div></section><section className="card-grid">{plan.map(x=><article className="panel" key={x.day}><span className="dashboard-kicker">Day {x.day}</span><h2>{x.topic}</h2><p>{subject} • {x.hours} focused hour{x.hours===1?'':'s'}</p><div className="progress-track"><span style={{width:`${Math.min(100,x.day*14)}%`}}/></div></article>)}</section></div>}
