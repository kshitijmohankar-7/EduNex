import{useState}from'react';export default function Communication(){const[m,setM]=useState([]),[text,setText]=useState('');const send=()=>{if(text.trim())setM([...m,{from:'You',text}]),setText('')};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Communication</span><h1>Student–Faculty Messages</h1><p>Keep academic conversations organized.</p></div></div><section className="panel"><div className="chat-messages">{m.length?m.map((x,i)=><div className="chat-bubble" key={i}><strong>{x.from}</strong><span>{x.text}</span></div>):<div className="tool-empty">Start a conversation with your faculty.</div>}</div><div className="quiz-toolbar"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Write a message..."/><button className="btn" onClick={send}>Send</button></div></section></div>}
