import{useState}from'react';
export default function Grievances(){const[tickets,setTickets]=useState([]);const[subject,setSubject]=useState('');const[detail,setDetail]=useState('');const submit=()=>{if(!subject.trim())return;setTickets([{id:Date.now(),subject,detail,status:'Open'},...tickets]);setSubject('');setDetail('')};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Student Services</span><h1>Grievance Desk</h1><p>Raise and track campus complaints from one place.</p></div></div><section className="panel tool-form"><label>Issue subject<input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="What do you need help with?"/></label><label>Details<textarea rows="5" value={detail} onChange={e=>setDetail(e.target.value)} placeholder="Describe the issue..."/></label><button className="btn" onClick={submit}>Create ticket</button></section><section className="panel"><h2>My tickets</h2>{tickets.length===0?<div className="tool-empty">No grievance tickets yet.</div>:tickets.map(t=><div className="academic-subject-card" key={t.id}><div className="academic-subject-top"><div><strong>{t.subject}</strong><small>{t.detail||'No additional details'}</small></div><span className="badge">{t.status}</span></div></div>)}</section></div>}
