import { useState } from 'react';
const cards=[['What is JVM?','The Java Virtual Machine executes Java bytecode.'],['What is encapsulation?','Bundling data and methods while controlling access to internal state.'],['What is an array?','A fixed-size indexed collection of values of the same declared type.'],['What is inheritance?','A mechanism where a class derives properties and behavior from another class.']];
export default function Flashcards(){const [i,setI]=useState(0);const [show,setShow]=useState(false);const [known,setKnown]=useState(0);const card=cards[i];const next=()=>{setShow(false);setI((i+1)%cards.length)};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">AI & Learning</span><h1>Flashcards</h1><p>Review important concepts with quick active-recall sessions.</p></div></div><section className="panel flashcard"><span className="dashboard-kicker">Card {i+1} of {cards.length}</span><h2>{show?card[1]:card[0]}</h2><button className="btn btn-outline" onClick={()=>setShow(!show)}>{show?'Hide answer':'Reveal answer'}</button></section><div className="toolbar"><button className="btn" onClick={()=>{setKnown(known+1);next()}}>I know it</button><button className="btn btn-outline" onClick={next}>Next card</button><span className="badge">Mastered this session: {known}</span></div></div>}
