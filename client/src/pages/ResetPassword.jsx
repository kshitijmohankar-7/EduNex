import{useState}from'react';import{api}from'../services/api';export default function ResetPassword(){const token=new URLSearchParams(location.search).get('token')||'', [password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');const submit=async e=>{e.preventDefault();if(password!==confirm)return setErr('Passwords do not match');try{const r=await api.resetPassword(token,password);setMsg(r.message)}catch(x){setErr(x.message)}};return <div className="page-shell"><div className="panel" style={{maxWidth:520,margin:'60px auto'}}><h1>Choose a new password</h1><form onSubmit={submit}><input type="password" minLength="8" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" required/><input type="password" minLength="8" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm password" required/><button className="btn" type="submit">Update password</button></form>{msg&&<p className="success-text">{msg}</p>}{err&&<p className="error-text">{err}</p>}</div></div>}