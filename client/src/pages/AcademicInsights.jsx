import{useEffect,useState}from'react';import{api}from'../services/api';
export default function AcademicInsights(){const[data,setData]=useState(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{api.getAcademicInsights().then(setData).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);if(loading)return <div className="panel">Generating academic insights...</div>;if(error)return <div className="panel"><div className="error-text">{error}</div></div>;if(!data)return null;return <div><div className="ledger-heading"><div><h2>AI Academic Insights</h2><p style={{margin:'6px 0',color:'var(--muted-text)'}}>Personalized insights calculated from your attendance, published marks and assignments.</p></div><span className="count">Dashboard analytics</span></div><hr className="ledger-rule"/><div className="card-grid"><div className="stat-card"><div className="stat-label">Overall attendance</div><div className="stat-value">{data.overallAttendance}%</div><div className="stat-sub">Target: 75%+</div></div><div className="stat-card"><div className="stat-label">Current academic index</div><div className="stat-value">{data.currentGpa}</div><div className="stat-sub">10-point weighted scale</div></div><div className="stat-card"><div className="stat-label">Assignments outstanding</div><div className="stat-value">{Number(data.assignmentSummary?.pending||0)+Number(data.assignmentSummary?.not_submitted||0)}</div><div className="stat-sub">Pending or not submitted</div></div></div><div className="panel"><h2>Recommended actions</h2><ul>{data.actions.map((x,i)=><li key={i} style={{marginBottom:8}}>{x}</li>)}</ul></div><div className="panel"><h2>Attendance by subject</h2><table className="ledger-table"><thead><tr><th>Subject</th><th>Present</th><th>Total</th><th>Attendance</th></tr></thead><tbody>{data.attendanceBySubject.map(x=><tr key={x.code}><td>{x.subject} <span className="code-stamp">{x.code}</span></td><td>{x.present}</td><td>{x.total}</td><td><span className={`pill ${x.percentage>=75?'pill-good':'pill-bad'}`}>{x.percentage}%</span></td></tr>)}</tbody></table></div><div className="panel"><h2>Latest performance by subject</h2><table className="ledger-table"><thead><tr><th>Subject</th><th>Exam</th><th>Percentage</th><th>Grade point</th></tr></thead><tbody>{data.performance.map((x,i)=><tr key={`${x.code}-${i}`}><td>{x.subject} <span className="code-stamp">{x.code}</span></td><td>{x.exam_type}</td><td>{x.percentage}%</td><td>{x.gradePoint}</td></tr>)}</tbody></table></div></div>}
