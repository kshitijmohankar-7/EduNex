import{useState}from'react';
const rows=[['Tuition Fee','₹42,000','Paid'],['Examination Fee','₹2,500','Pending'],['Library Fee','₹1,000','Paid']];export default function Fees(){const[paid,setPaid]=useState([]);const total=rows.reduce((s,r)=>s+Number(r[1].replace(/[^0-9]/g,'')),0);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Finance</span><h1>Fees & Finance</h1><p>View your fee summary and outstanding payments.</p></div></div><div className="card-grid"><div className="stat-card"><div className="stat-label">Semester fees</div><div className="stat-value">₹{total.toLocaleString()}</div></div><div className="stat-card"><div className="stat-label">Pending items</div><div className="stat-value">{rows.filter(r=>r[2]==='Pending'&&!paid.includes(r[0])).length}</div></div></div><section className="panel">{rows.map(([name,amount,status])=><div className="academic-subject-card" key={name}><div className="academic-subject-top"><div><strong>{name}</strong><small>{amount}</small></div><span className="badge">{paid.includes(name)?'Paid':status}</span></div>{status==='Pending'&&!paid.includes(name)&&<button className="btn" onClick={()=>setPaid([...paid,name])}>Pay now</button>}</div>)}</section></div>}
