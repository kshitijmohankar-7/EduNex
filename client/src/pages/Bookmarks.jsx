import{useState}from'react';export default function Bookmarks(){const[b,setB]=useState(['Java OOP Guide','DSA Revision']);const[x,setX]=useState('');return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Learning</span><h1>Bookmarks</h1><p>Keep important study resources one click away.</p></div></div><section className="panel"><input className="search-input" value={x} onChange={e=>setX(e.target.value)} placeholder="Add bookmark title"/><button className="btn" onClick={()=>{if(x.trim())setB([...b,x.trim()]);setX('')}}>Add</button>{b.map(v=><div className="academic-subject-card" key={v}>{v}</div>)}</section></div>}
