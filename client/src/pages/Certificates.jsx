import{useState}from'react';
const seed=[['React Workshop','Participation','Verified'],['AIXplore Hackathon','Achievement','Verified']];export default function Certificates(){const[items]=useState(seed);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Career</span><h1>Certificates</h1><p>Keep your verified academic and extracurricular credentials organized.</p></div></div><section className="panel">{items.map(([name,type,status])=><div className="academic-subject-card" key={name}><div className="academic-subject-top"><div><strong>{name}</strong><small>{type}</small></div><span className="badge">{status}</span></div><button className="btn btn-outline" onClick={()=>alert(`Verification ID: ${name.toLowerCase().replaceAll(' ','-')}`)}>Verify</button></div>)}</section></div>}
