import{useMemo}from'react';export default function ProfileCompleteness(){const items=['Basic profile','Education details','Skills','Achievements','Career preferences'];const done=3;const pct=useMemo(()=>Math.round(done/items.length*100),[]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Profile</span><h1>Profile Completeness</h1><p>Complete your profile to improve recommendations.</p></div></div><section className="panel"><div className="stat-value">{pct}%</div>{items.map((x,i)=><div className="academic-subject-card" key={x}>{i<done?'✓':'○'} {x}</div>)}</section></div>}
