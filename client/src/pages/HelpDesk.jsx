import{useEffect,useState}from'react';import{api}from'../services/api';export default function HelpDesk(){const[t,setT]=useState([]),[subject,setSubject]=useState(''),[q,setQ]=useState(''),[priority,setPriority]=useState('normal'),[err,setErr]=useState('');const load=async()=>{try{setT((await api.getHelpDeskTickets()).items||[])}catch(e){setErr(e.message)}};useEffect(()=>{load()},[]);const add=async()=>{try{await api.createHelpDeskTicket({subject,description:q,priority});setSubject('');setQ('');await load()}catch(e){setErr(e.message)}};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Support</span><h1>Help Desk</h1><p>Track support requests from your account.</p></div></div><section className="panel"><input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Issue subject"/><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Describe your issue"/><select value={priority} onChange={e=>setPriority(e.target.value)}><option>low</option><option>normal</option><option>high</option><option>urgent</option></select><button className="btn" onClick={add}>Submit support request</button>{err&&<p className="error-text">{err}</p>}</section><section className="panel"><h2>Requests</h2>{t.length?t.map(x=><div className="academic-subject-card" key={x.id}><strong>{x.subject}</strong><span>{x.description}</span><span className="badge">{x.status}</span></div>):<div className="tool-empty">No support requests.</div>}</section></div>}