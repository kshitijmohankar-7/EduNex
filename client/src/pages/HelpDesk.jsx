import{useState}from'react';export default function HelpDesk(){const[t,setT]=useState([]),[q,setQ]=useState('');const add=()=>{if(q.trim())setT([{id:Date.now(),q,status:'Open'},...t]),setQ('')};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Support</span><h1>Help Desk</h1><p>Get help with EduNex and campus services.</p></div></div><section className="panel"><label>Describe your issue<input value={q} onChange={e=>setQ(e.target.value)} placeholder="How can we help?"/></label><button className="btn" onClick={add}>Submit support request</button></section><section className="panel"><h2>Requests</h2>{t.length?t.map(x=><div className="academic-subject-card" key={x.id}><strong>{x.q}</strong><span className="badge">{x.status}</span></div>):<div className="tool-empty">No support requests.</div>}</section></div>}
