import{useState}from'react';export default function DeadlineTracker(){const[d,setD]=useState('');const[list,setList]=useState([['DSA Assignment','2026-09-20'],['OS Quiz','2026-09-24']]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Planning</span><h1>Deadline Tracker</h1><p>Track important academic deadlines in one place.</p></div></div><section className="panel"><input className="search-input" value={d} onChange={e=>setD(e.target.value)} placeholder="Deadline title"/><button className="btn" onClick={()=>{if(d.trim())setList([...list,[d.trim(),'Set reminder']]);setD('')}}>Add deadline</button>{list.map(x=><div className="academic-subject-card" key={x[0]}><strong>{x[0]}</strong><small>{x[1]}</small></div>)}</section></div>}
