import{useMemo,useState}from'react';
const books=[['Java Programming','Computer Science','Available'],['Database Systems','Computer Science','Available'],['Operating Systems','Computer Science','Issued'],['Web Development','Computer Science','Available']];export default function Library(){const[q,setQ]=useState('');const[issued,setIssued]=useState([]);const filtered=useMemo(()=>books.filter(b=>b[0].toLowerCase().includes(q.toLowerCase())),[q]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Campus Services</span><h1>Digital Library</h1><p>Search resources and track your issued books.</p></div></div><section className="panel"><input className="search-input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search books..."/>{filtered.map(([title,cat,status])=><div className="academic-subject-card" key={title}><div className="academic-subject-top"><div><strong>{title}</strong><small>{cat}</small></div><span className="badge">{issued.includes(title)?'Requested':status}</span></div>{status==='Available'&&!issued.includes(title)&&<button className="btn btn-outline" onClick={()=>setIssued([...issued,title])}>Request issue</button>}</div>)}</section></div>}
