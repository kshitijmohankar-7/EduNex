import{useState}from'react';
const seed=[['Hackathon','Build and pitch your project with fellow students.','Sep 20'],['Workshop','Hands-on web development workshop.','Sep 24'],['Seminar','Career and placement preparation session.','Sep 28']];export default function Events(){const[events,setEvents]=useState(seed);const[registered,setRegistered]=useState([]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Campus Life</span><h1>Events</h1><p>Discover workshops, hackathons, seminars and student activities.</p></div></div><div className="academic-subject-grid">{events.map(([title,desc,date])=><article className="panel" key={title}><span className="dashboard-kicker">{date}</span><h2>{title}</h2><p>{desc}</p><button className="btn" onClick={()=>setRegistered([...new Set([...registered,title])])}>{registered.includes(title)?'Registered':'Register'}</button></article>)}</div><p className="badge">{registered.length} event{registered.length===1?'':'s'} registered</p></div>}
