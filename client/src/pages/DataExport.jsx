import{useState}from'react';export default function DataExport(){const[e,setE]=useState(false);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Account</span><h1>Data Export</h1><p>Prepare a portable copy of your EduNex information.</p></div></div><section className="panel"><h2>Student data</h2><p>Profile, academic summaries, achievements and preferences.</p><button className="btn" onClick={()=>setE(true)}>{e?'Export requested':'Request export'}</button></section></div>}
