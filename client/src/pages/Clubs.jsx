import{useState}from'react';
const clubs=[['Coding Club','Build software, solve problems and collaborate.'],['AI & Data Club','Explore AI, ML and data projects.'],['Sports Club','Stay active and join campus sports activities.']];export default function Clubs(){const[joined,setJoined]=useState([]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Campus Life</span><h1>Clubs & Communities</h1><p>Find communities that match your interests.</p></div></div><div className="card-grid">{clubs.map(([name,desc])=><article className="panel" key={name}><h2>{name}</h2><p>{desc}</p><button className="btn" onClick={()=>setJoined([...new Set([...joined,name])])}>{joined.includes(name)?'Joined':'Join club'}</button></article>)}</div></div>}
