import{useState}from'react';const seed=[['Frontend Intern','Applied'],['Software Engineer Intern','Shortlisted'],['Data Analyst Intern','Applied']];export default function PlacementTracker(){const[s,setS]=useState(seed);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Placements</span><h1>Application Tracker</h1><p>Follow every placement application from one dashboard.</p></div></div><section className="panel">{s.map(([n,v],i)=><div className="academic-subject-card" key={n}><strong>{n}</strong><small>{v}</small><button className="btn" onClick={()=>setS(s.map((x,j)=>j===i?[x[0],x[1]==='Applied'?'Shortlisted':'Interview']:x))}>Advance</button></div>)}</section></div>}
