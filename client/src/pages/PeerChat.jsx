import{useState}from'react';export default function PeerChat(){const[m,setM]=useState('');const[list,setList]=useState(['Welcome to EduNex peer chat.']);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Community</span><h1>Peer Chat</h1><p>Share questions and collaborate with classmates.</p></div></div><section className="panel">{list.map((x,i)=><p key={i}>{x}</p>)}<input className="search-input" value={m} onChange={e=>setM(e.target.value)} placeholder="Write a message..."/><button className="btn" onClick={()=>{if(m.trim())setList([...list,m.trim()]);setM('')}}>Send</button></section></div>}
