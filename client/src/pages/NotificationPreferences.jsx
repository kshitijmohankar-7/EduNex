import{useState}from'react';export default function NotificationPreferences(){const[a,setA]=useState(true),[e,setE]=useState(true),[p,setP]=useState(false);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Settings</span><h1>Notification Preferences</h1><p>Choose which EduNex alerts you want to receive.</p></div></div><section className="panel">{[['Academic alerts',a,setA],['Email updates',e,setE],['Placement alerts',p,setP]].map(([n,v,s])=><label className="academic-subject-card" key={n}><input type="checkbox" checked={v} onChange={x=>s(x.target.checked)}/> {n}</label>)}</section></div>}
