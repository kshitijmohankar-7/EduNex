import{useState}from'react';
const seed=['Java','C','HTML/CSS','JavaScript','React','Git/GitHub'];
export default function SkillProfile(){const[skills,setSkills]=useState(seed);const[value,setValue]=useState('');const add=()=>{const s=value.trim();if(s&&!skills.includes(s))setSkills([...skills,s]);setValue('')};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Student Success</span><h1>Skill Profile</h1><p>Keep your technical and professional skills ready for projects and placements.</p></div></div><section className="panel"><h2>My skills</h2><div className="quiz-toolbar"><input value={value} onChange={e=>setValue(e.target.value)} onKeyDown={e=>e.key==='Enter'&&add()} placeholder="Add a skill"/><button className="btn" onClick={add}>Add skill</button></div><div className="skill-chip-grid">{skills.map(s=><span className="skill-chip" key={s}>{s}<button onClick={()=>setSkills(skills.filter(x=>x!==s))}>×</button></span>)}</div></section><section className="card-grid"><div className="stat-card"><div className="stat-label">Technical skills</div><div className="stat-value">{skills.length}</div></div><div className="stat-card"><div className="stat-label">Profile status</div><div className="stat-value">{skills.length>=5?'Strong':'Build it'}</div></div><div className="stat-card"><div className="stat-label">Next step</div><div className="stat-value">Add projects</div></div></section></div>}
