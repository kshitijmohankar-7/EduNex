import{useEffect,useState}from'react';export default function FocusTimer(){const[sec,setSec]=useState(25*60),[run,setRun]=useState(false);useEffect(()=>{if(!run)return;const id=setInterval(()=>setSec(s=>Math.max(0,s-1)),1000);return()=>clearInterval(id)},[run]);const m=String(Math.floor(sec/60)).padStart(2,'0'),s=String(sec%60).padStart(2,'0');return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Productivity</span><h1>Focus Timer</h1><p>Use a focused 25-minute study session.</p></div></div><section className="panel"><div className="stat-value">{m}:{s}</div><button className="btn" onClick={()=>setRun(!run)}>{run?'Pause':'Start focus'}</button><button className="btn" onClick={()=>{setRun(false);setSec(1500)}}>Reset</button></section></div>}
