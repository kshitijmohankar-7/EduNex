import{useState}from'react';export default function StudentSettings(){const[settings,setSettings]=useState({email:true,assignment:true,attendance:true});const flip=k=>setSettings({...settings,[k]:!settings[k]});return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Preferences</span><h1>Notification Settings</h1><p>Control which academic alerts you receive.</p></div></div><section className="panel">{Object.entries(settings).map(([k,v])=><div className="academic-subject-card" key={k}><div><strong>{k[0].toUpperCase()+k.slice(1)} alerts</strong><small>EduNex notifications</small></div><button className="btn" onClick={()=>flip(k)}>{v?'Enabled':'Disabled'}</button></div>)}</section></div>}
