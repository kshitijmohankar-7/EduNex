import{useEffect,useState}from'react';export default function StudyTimer(){const[s,setS]=useState(25*60),[running,setRunning]=useState(false);useEffect(()=>{if(!running)return;const id=setInterval(()=>setS(v=>v>0?v-1:0),1000);return()=>clearInterval(id)},[running]);const reset=()=>{setRunning(false);setS(25*60)};return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Productivity</span><h1>Focus Timer</h1><p>Use focused study sessions to build consistent habits.</p></div></div><section className="panel" style={{textAlign:'center'}}><div className="stat-value">{String(Math.floor(s/60)).padStart(2,'0')}:{String(s%60).padStart(2,'0')}</div><p>25-minute focus session</p><button className="btn" onClick={()=>setRunning(!running)}>{running?'Pause':'Start'}</button> <button className="btn btn-outline" onClick={reset}>Reset</button></section></div>}
