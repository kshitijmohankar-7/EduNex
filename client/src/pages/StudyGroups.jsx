import{useState}from'react';const groups=['DSA Practice','React Builders','Placement Prep'];export default function StudyGroups(){const[joined,setJoined]=useState([]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Learning</span><h1>Study Groups</h1><p>Learn together with focused peer groups.</p></div></div><div className="card-grid">{groups.map(g=><article className="panel" key={g}><h2>{g}</h2><p>Weekly peer learning and problem-solving sessions.</p><button className="btn" onClick={()=>setJoined([...new Set([...joined,g])])}>{joined.includes(g)?'Joined':'Join group'}</button></article>)}</div></div>}
