import{useState}from'react';export default function NotesHub(){const[n,setN]=useState('');const[list,setList]=useState([]);return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Learning</span><h1>Notes Hub</h1><p>Create quick personal study notes.</p></div></div><section className="panel"><textarea className="search-input" rows="5" value={n} onChange={e=>setN(e.target.value)} placeholder="Write a study note..."/><button className="btn" onClick={()=>{if(n.trim())setList([...list,n.trim()]);setN('')}}>Save note</button>{list.map((x,i)=><div className="academic-subject-card" key={i}>{x}</div>)}</section></div>}
