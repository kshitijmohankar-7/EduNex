import{useState}from'react';
export default function ResumeBuilder(){const[r,setR]=useState({name:'',email:'',summary:'',skills:''});const[u,setU]=useState(false);const change=e=>setR({...r,[e.target.name]:e.target.value});return <div className="page-shell"><div className="page-heading"><div><span className="dashboard-kicker">Career</span><h1>Resume Builder</h1><p>Create a clean resume draft from your EduNex profile.</p></div></div><div className="tool-grid"><section className="panel tool-form"><label>Full name<input name="name" value={r.name} onChange={change} placeholder="Your name"/></label><label>Email<input name="email" value={r.email} onChange={change} placeholder="you@example.com"/></label><label>Professional summary<textarea name="summary" rows="4" value={r.summary} onChange={change} placeholder="Short profile summary"/></label><label>Skills<textarea name="skills" rows="3" value={r.skills} onChange={change} placeholder="Java, React, SQL..."/></label><button className="btn" onClick={()=>setU(true)}>Preview resume</button></section><section className="panel tool-output">{u?<><span className="dashboard-kicker">Resume Preview</span><h2>{r.name||'Your Name'}</h2><p>{r.email||'email@example.com'}</p><hr/><h3>Profile</h3><p>{r.summary||'Add a professional summary.'}</p><h3>Skills</h3><p>{r.skills||'Add your skills.'}</p></>:<div className="tool-empty"><strong>Build your resume</strong><span>Fill the form and preview it here.</span></div>}</section></div></div>}
