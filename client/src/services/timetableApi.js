const BASE_URL=import.meta.env.VITE_API_BASE_URL||'http://localhost:5000/api';async function request(path,options={}){const token=localStorage.getItem('edunex_token');const headers={...(token?{Authorization:`Bearer ${token}`}:{})};if(!(options.body instanceof FormData))headers['Content-Type']='application/json';const r=await fetch(`${BASE_URL}${path}`,{...options,headers});if(!r.ok){const b=await r.json().catch(()=>({}));throw new Error(b.error||`Request failed with status ${r.status}`)}return r.json()}export const timetableApi={list:()=>request('/timetable'),create:d=>request('/timetable',{method:'POST',body:JSON.stringify(d)}),update:(id,d)=>request(`/timetable/${id}`,{method:'PATCH',body:JSON.stringify(d)}),remove:id=>request(`/timetable/${id}`,{method:'DELETE'})};