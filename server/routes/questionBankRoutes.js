const express=require('express');const multer=require('multer');const authenticate=require('../middleware/auth');const authorize=require('../middleware/roleCheck');const controller=require('../controllers/questionBankController');const router=express.Router();const upload=multer({storage:multer.memoryStorage(),fileFilter:(_req,file,cb)=>{const allowed=['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document'];cb(allowed.includes(file.mimetype)?null:new Error('Only PDF, DOC and DOCX files are allowed.'),allowed.includes(file.mimetype))},limits:{fileSize:10*1024*1024}});router.use(authenticate);router.get('/',authorize('student','faculty'),controller.listQuestionBanks);router.post('/',authorize('faculty'),upload.single('file'),controller.createQuestionBank);router.delete('/:id',authorize('faculty'),controller.deleteQuestionBank);module.exports=router;