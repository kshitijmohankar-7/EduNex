const express=require('express');const path=require('path');const multer=require('multer');const router=express.Router();const authenticate=require('../middleware/auth');const authorize=require('../middleware/roleCheck');const{getMaterials,createMaterial,reindexMaterial}=require('../controllers/materialController');
const upload=multer({storage:multer.memoryStorage(),fileFilter:(_req,file,cb)=>{const allowed=['application/pdf','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document'];cb(allowed.includes(file.mimetype)?null:new Error('Only PDF, DOC and DOCX files are allowed.'),allowed.includes(file.mimetype));},limits:{fileSize:10*1024*1024}});
router.get('/',authenticate,authorize('student','faculty'),getMaterials);router.post('/',authenticate,authorize('faculty'),upload.single('file'),createMaterial);router.post('/:id/reindex',authenticate,authorize('faculty'),reindexMaterial);module.exports=router;
