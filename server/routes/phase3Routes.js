const express=require('express');const router=express.Router();const authenticate=require('../middleware/auth');const authorize=require('../middleware/roleCheck');const{studentAnalytics,facultyAnalytics,adminAnalytics}=require('../controllers/phase3Controller');router.get('/student',authenticate,authorize('student'),studentAnalytics);router.get('/faculty',authenticate,authorize('faculty'),facultyAnalytics);router.get('/admin',authenticate,authorize('admin'),adminAnalytics);module.exports=router;module.exports={router};