const pool=require('../config/db');const path=require('path');
function publicUploadPath(filePath,folder='assignments'){if(!filePath)return filePath;if(String(filePath).startsWith('/uploads/'))return filePath;const normalized=String(filePath).replace(/\\/g,'/');const marker=`/uploads/${folder}/`;const index=normalized.toLowerCase().indexOf(marker);if(index>=0)return normalized.slice(index);return `/uploads/${folder}/${path.basename(normalized)}`}
async function getFacultyByUserId(userId){const r=await pool.query('SELECT id FROM faculty WHERE user_id=$1',[userId]);return r.rows[0]||null}
async function getAssignments(req,res,next){try{const{subjectId}=req.query;let query=`SELECT a.id,a.subject_id,a.title,a.description,a.file_path,a.file_type,a.issue_date,a.deadline,s.name AS subject,s.code,sub.id AS submission_id,sub.status AS submission_status,sub.submitted_at AS submission_date FROM assignments a JOIN subjects s ON s.id=a.subject_id JOIN students st ON st.user_id=$1 JOIN student_subjects ss ON ss.student_id=st.id AND ss.subject_id=a.subject_id AND ss.status='approved' LEFT JOIN assignment_submissions sub ON sub.assignment_id=a.id AND sub.student_id=st.id`;const values=[req.user.id];if(subjectId){query+=` WHERE a.subject_id=$2`;values.push(subjectId)}query+=` ORDER BY a.deadline ASC,a.issue_date DESC`;const result=await pool.query(query,values);res.json(result.rows.map(row=>({...row,file_path:publicUploadPath(row.file_path,'assignments')})));}catch(err){next(err)}}
async function createAssignment(req,res,next){try{const{subjectId,title,description,deadline}=req.body;if(!subjectId||!title||!deadline)return res.status(400).json({error:'subjectId, title and deadline are required'});if(!req.file)return res.status(400).json({error:'Assignment file is required'});const faculty=await getFacultyByUserId(req.user.id);if(!faculty)return res.status(404).json({error:'Faculty profile not found'});const subjectResult=await pool.query('SELECT id FROM subjects WHERE id=$1',[subjectId]);if(!subjectResult.rows.length)return res.status(404).json({error:'Subject not found'});const relativePath=`/uploads/assignments/${req.file.filename}`;const result=await pool.query(`INSERT INTO assignments(subject_id,title,description,file_path,file_type,issue_date,deadline,uploaded_by) VALUES($1,$2,$3,$4,$5,CURRENT_TIMESTAMP,$6,$7) RETURNING id,subject_id,title,description,file_path,file_type,issue_date,deadline,uploaded_by`,[subjectId,title.trim(),description||null,relativePath,req.file.mimetype,deadline,faculty.id]);res.status(201).json({message:'Assignment uploaded successfully',assignment:result.rows[0]});}catch(err){next(err)}}
async function submitAssignment(req,res,next){try{const{assignmentId}=req.params;const studentResult=await pool.query('SELECT id FROM students WHERE user_id=$1',[req.user.id]);if(!studentResult.rows.length)return res.status(404).json({error:'Student profile not found'});const studentId=studentResult.rows[0].id;const assignmentResult=await pool.query('SELECT id,deadline FROM assignments WHERE id=$1',[assignmentId]);if(!assignmentResult.rows.length)return res.status(404).json({error:'Assignment not found'});if(new Date()>new Date(assignmentResult.rows[0].deadline))return res.status(400).json({error:'Assignment deadline has passed'});if(!req.file)return res.status(400).json({error:'Please upload your assignment file'});const existing=await pool.query('SELECT id,status FROM assignment_submissions WHERE assignment_id=$1 AND student_id=$2',[assignmentId,studentId]);if(existing.rows.length){const submission=existing.rows[0];if(submission.status==='pending')return res.status(400).json({error:'Your submission is already waiting for faculty approval'});if(submission.status==='submitted'||submission.status==='approved')return res.status(400).json({error:'This assignment has already been approved'});if(submission.status==='rejected'){const r=await pool.query(`UPDATE assignment_submissions SET file_path=$1,status='pending',submitted_at=CURRENT_TIMESTAMP WHERE id=$2 RETURNING *`,[`/uploads/assignment-submissions/${req.file.filename}`,submission.id]);return res.status(200).json({message:'Assignment resubmitted successfully. Waiting for faculty approval.',submission:r.rows[0]})}}const result=await pool.query(`INSERT INTO assignment_submissions(assignment_id,student_id,file_path,status,submitted_at) VALUES($1,$2,$3,'pending',CURRENT_TIMESTAMP) RETURNING *`,[assignmentId,studentId,`/uploads/assignment-submissions/${req.file.filename}`]);res.status(201).json({message:'Assignment submitted successfully. Waiting for faculty approval.',submission:result.rows[0]});}catch(err){next(err)}}
async function getAssignmentSubmissions(req,res,next){try{const{assignmentId}=req.params;const result=await pool.query(`SELECT sub.id,sub.assignment_id,sub.student_id,sub.file_path,sub.status,sub.marks_obtained,sub.submitted_at,a.title AS assignment_title,a.deadline,s.student_code,u.full_name AS student_name FROM assignment_submissions sub JOIN assignments a ON a.id=sub.assignment_id JOIN students s ON s.id=sub.student_id JOIN users u ON u.id=s.user_id WHERE sub.assignment_id=$1 ORDER BY sub.submitted_at DESC`,[assignmentId]);res.json(result.rows.map(row=>({...row,file_path:publicUploadPath(row.file_path,'assignment-submissions')})));}catch(err){next(err)}}
async function approveAssignmentSubmission(req,res,next){try{const{submissionId}=req.params;const result=await pool.query(`UPDATE assignment_submissions SET status='submitted' WHERE id=$1 AND status='pending' RETURNING *`,[submissionId]);if(!result.rows.length)return res.status(404).json({error:'Pending submission not found'});res.json({message:'Assignment submission approved',submission:result.rows[0]});}catch(err){next(err)}}
async function rejectAssignmentSubmission(req,res,next){try{const{submissionId}=req.params;const result=await pool.query(`UPDATE assignment_submissions SET status='rejected' WHERE id=$1 AND status='pending' RETURNING *`,[submissionId]);if(!result.rows.length)return res.status(404).json({error:'Pending submission not found'});res.json({message:'Assignment submission rejected',submission:result.rows[0]});}catch(err){next(err)}}
async function getFacultyAssignments(req,res,next){try{const faculty=await getFacultyByUserId(req.user.id);if(!faculty)return res.status(404).json({error:'Faculty profile not found'});const result=await pool.query(`SELECT a.id,a.subject_id,a.title,a.description,a.instructions,a.file_path,a.file_name,a.file_type,a.issue_date,a.deadline,s.name AS subject,s.code,COUNT(sub.id) AS submission_count FROM assignments a JOIN subjects s ON s.id=a.subject_id JOIN faculty_subject_assignments fsa ON fsa.subject_id=a.subject_id AND fsa.faculty_id=$1 LEFT JOIN assignment_submissions sub ON sub.assignment_id=a.id GROUP BY a.id,a.subject_id,a.title,a.description,a.instructions,a.file_path,a.file_name,a.file_type,a.issue_date,a.deadline,s.name,s.code ORDER BY a.deadline ASC,a.issue_date DESC`,[faculty.id]);res.json(result.rows.map(row=>({...row,file_path:publicUploadPath(row.file_path,'assignments')})));}catch(err){next(err)}}
module.exports={getAssignments,createAssignment,submitAssignment,getAssignmentSubmissions,approveAssignmentSubmission,rejectAssignmentSubmission,getFacultyAssignments};